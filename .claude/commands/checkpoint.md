Update PROGRESS.md to reflect the current state of the repository.

1. Set "Last updated" to today and "Current task" to the next task to work on.
2. Update the status table. Only mark a task `done` if its definition of done
   in TODO.md actually passes right now. Run the check; do not assume.
3. Rewrite "What exists right now" from the actual filesystem, not from
   memory. One line per module.
4. Add anything stubbed, worked around, or left incomplete to "Known issues".
   Be specific. Include file paths.
5. If you made a design decision not recorded in docs/decisions.md, add it to
   "Decisions made during implementation", and if it is significant, add it to
   docs/decisions.md too and say so.
6. Prepend a session log entry: date, task id, what changed, what is next.

Then print a three line summary: what got done, what is blocked, what is next.
